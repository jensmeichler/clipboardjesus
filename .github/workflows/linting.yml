name: 👕 Lint source files

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Lint source files
        run: npm run lint
