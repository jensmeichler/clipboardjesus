name: 👕 Lint source files

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies 📦
        run: npm install

      - name: Lint source files 👕
        run: npm run lint
